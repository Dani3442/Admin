version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: product_admin_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: product_admin
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: product_admin_app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: "postgresql://postgres:postgres123@postgres:5432/product_admin"
      AUTH_SECRET: "your-super-secret-key-32-chars-min-change-this"
      NEXTAUTH_URL: "http://localhost:3000"
      ADMIN_EMAIL: "admin@company.com"
      ADMIN_PASSWORD: "Admin1234!"
      ADMIN_NAME: "Данила"
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
